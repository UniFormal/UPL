[{"cell_type":"markdown","source":["## Experimenting with the semi-synthetic geometry of FrameIT"]},{"cell_type":"markdown","source":["UPL currently has only rationals, but trigonometry is not closed undert the rationals. So we use a type alias for now, to seamlessly phase them out later."]},{"cell_type":"code","source":["synth_geometry.tmp"]},{"cell_type":"markdown","source":["Points just are. But we may find a way to measure (some of) their coordinates, which makes them partially measured points `Ppoint`. If we know all of their coordinates, we call them a `Position`."]},{"cell_type":"code","source":["//theory Point{}","theory Ppoint{","    x: rat","    y: rat","}","theory Position{","    include Ppoint","    z: rat","}","","tmp = Ppoint{x=1, y=0}"]},{"cell_type":"markdown","source":["We can project a `Ppoint` to the ground with the `pendulum` to find the `Position` below it."]},{"cell_type":"code","source":["pendulum = (p: Ppoint) -> Position{x= p.x, y= p.y, z=0}"]},{"cell_type":"code","source":["{","    tmp1 = pendulum(Ppoint{x=1, y=0})","    }"]},{"cell_type":"code","source":["","tmp2 = pendulum(Position{x=1, y=0, z=10})"]},{"cell_type":"code","source":[""]}]